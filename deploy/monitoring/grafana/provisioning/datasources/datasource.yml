# Grafana 데이터소스 provisioning — 콜로케이트된 Prometheus를 기본 소스로 등록 (#88)
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "15s"
