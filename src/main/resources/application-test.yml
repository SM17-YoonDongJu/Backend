spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test_db
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop   # 테스트마다 스키마 생성
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: false            # 테스트에선 Flyway 비활성
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092
    security:
      protocol: PLAINTEXT
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: dummy
            client-secret: dummy
          naver:
            client-id: dummy
            client-secret: dummy

jwt:
  secret: testsecretkeyforcitestingonly32chars!!

aws:
  region: ap-northeast-2
  s3:
    bucket: test-bucket