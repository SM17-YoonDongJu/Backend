-- 사건번호(case_no) 발급용 전역 시퀀스.
-- nextval은 무락·원자·비트랜잭셔널이라 동시 생성에도 서로 막지 않고 유일값을 보장한다
-- (사전확인·재시도·행 락 불필요). 롤백 시 번호는 회수되지 않아 gap이 생길 수 있으나(연번 보장 안 함),
-- 사람이 부르는 식별자에 연번이 필수는 아니라 허용한다. 날짜별로 리셋하지 않는다 — 리셋은 조율을
-- 재도입해 무경합 목적을 깬다(유일성은 시퀀스 하나로 보장, 날짜는 사람용 접두어).
CREATE SEQUENCE core.report_case_seq;
