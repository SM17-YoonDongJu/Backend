-- =====================================================================
-- ai 스키마 제거 — "GRANT 미적용·계약 테이블 미배포" 상태를 시뮬레이션하는 테스트 전용 스크립트.
--
-- design.md §8 E14 검증용: ai.ocr_job_failures를 읽을 수 없을 때
--   (1) 앱은 정상 기동하고(@Subselect라 ddl-auto: validate 대상이 아님)
--   (2) 목록·상세는 200 + PROCESSING으로 degrade하며
--   (3) 전용 엔드포인트만 500이 되는지
-- 를 실제 SQL 실패로 확인한다.
--
-- ⚠️ 이 스크립트를 쓰는 테스트 클래스는 종료 시 /sql/ai-contract-schema.sql로 스키마를 반드시 되돌린다
--    (AFTER_TEST_CLASS). 운영/개발 DB에는 절대 적용하지 않는다.
-- =====================================================================
DROP SCHEMA IF EXISTS ai CASCADE;
